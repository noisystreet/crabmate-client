---
description: 提交代码前须运行 pre-commit，通过后再 git commit
alwaysApply: true
---

# 提交前：pre-commit

代表用户执行 **`git commit`** 之前，必须先完成本仓检查。

## 必须执行

优先（仓库根）：

```bash
pre-commit run --all-files
```

未安装 `pre-commit` 时：

```bash
bash scripts/check.sh
```

复杂度钩子另需 **`pip install lizard`**（与主仓一致）。  
`ktlint-android` 另需本机 **java**（OpenJDK 17+；首次会下载钉死的 ktlint CLI）。

## 流程

- 失败则修复后重跑，直至通过，再 `git commit`
- **禁止** `--no-verify`
- commit subject：Conventional Commits + 中英双语（见 `conventional-commits.mdc`）

## 与本仓对应

钩子见 **`.pre-commit-config.yaml`**（对齐主仓：分项 fmt/clippy、`check-no-main-path`、lizard（各模块 CCN>10 函数个数，实测须等于 cap；变小须调低 cap / `--write-caps`）/ fn-param / fn-nloc、可选 taplo、`ktlint-android`、typos、commit-msg）。  
**不含** Server 主仓的 frontend wasm、全 workspace clippy、Playwright。  
**`cargo test` / Victauri E2E 不在 pre-commit**（见 CI 与 `docs/TESTING.md`）。
