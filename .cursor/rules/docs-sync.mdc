---
description: 壳行为或架构变更时同步本仓文档；契约/协议只链主仓
alwaysApply: true
---

# 文档同步（crabmate-client）

## 本仓须更新

- 用户可见壳行为 → `README.md` 与对应 `desktop-tauri/` / `mobile-tauri/` README
- 连接模型、生命周期、托盘/单实例 → `docs/design/tauri_gui_mvp_design.md`
- 人工壳冒烟步骤 → `docs/design/shell_smoke_runbook.md`
- 测试/检查入口 → `docs/TESTING.md`、`AGENTS.md` Required Commands

## 勿在本仓「另起炉灶」

以下变更写 **Server 主仓**，本仓最多加链接：

- SSE / HTTP 契约、错误码、`request_id`
- CORS、`CM_WEB_*`、API 基址语义
- `client-contract-v*` 发版策略
- 路径 A ADR / Phase todo 勾选

## 禁止

- 把 `agent_space/` 草稿当作权威文档引用
- 用 path 依赖回主开发树「顺便」改主仓文档冒充已同步
