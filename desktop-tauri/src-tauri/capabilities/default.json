{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口：核心 API、Victauri 调试、对话框与系统浏览器打开链接。remote 仅回环：远程 serve 无桌面 IPC（外链走 window.open 回退）。",
  "windows": ["main", "default"],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*"
    ]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "window-state:default"
  ]
}
