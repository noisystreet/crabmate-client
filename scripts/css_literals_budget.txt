# CSS 字面量预算（门禁：scripts/check-css-literals.sh）
#
# 扫描范围：frontend/styles/*.css，排除 tokens.css（token 定义处）；themes/*.css 不扫描（本就定义颜色）。
#
# 格式：<相对路径>  font-size=<n>  color=<n>   # <理由 / 清理计划>
#   - 缺 '# 理由' / 重复登记 / 字段缺失：门禁报格式错误；
#   - 实际值必须等于预算值：写多写少都 fail（清理后不同步下调，条目就成了「隐性豁免」）；
#   - 未登记的文件预算视为 0；两项均为 0 的条目、或文件已不存在的条目，报预算失效，须删除。
#
# 目标是把存量逐文件清到 0，清理一处就下调一处预算。
# 当前进度：font-size 已全量清到 0（Phase 1a，字号统一走 tokens.css 的 --text-* 档）；
# 颜色已清 alert 五色、--danger 兼容别名（Phase 1b，语义色归一）与 inset 高光 / 强调底前景
# （Phase 1c，走 --inset-highlight / --on-accent），故仅剩遮罩与浮层阴影。

frontend/styles/layout-chat.css     font-size=0   color=1    # 1 处 rgba(0,0,0,.12) 浮层阴影待阴影 token
frontend/styles/mobile.css          font-size=0   color=1    # 1 处 rgba(0,0,0,.48) 抽屉遮罩待 token 化
frontend/styles/modal.css           font-size=0   color=1    # 1 处 rgb(0 0 0 / 18%) 浮层阴影待 token 化
frontend/styles/shell-ds.css        font-size=0   color=3    # 2 处 color-mix(#000 …) 阴影 + 1 处 rgba(0,0,0,.48) 遮罩待 token 化
frontend/styles/shell-topbar.css    font-size=0   color=2    # 2 处 rgba(0,0,0,.12) 浮层阴影待 token 化
frontend/styles/sidebar.css         font-size=0   color=1    # 1 处 rgba(0,0,0,.5) 遮罩待 token 化
frontend/styles/tauri-shell.css     font-size=0   color=2    # #c42b1c / #fff 为 Windows 关闭键红底白字，壳层专用
